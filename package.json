{
  "name": "amanwal-monorepo",
  "version": "1.0.0",
  "description": "Plataforma de alojamiento de cabañas",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend",
    "db:setup": "npm run prisma:migrate -w backend && npm run prisma:generate -w backend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/cors": "^2.8.19",
    "@types/validator": "^13.15.10"
  }
}
